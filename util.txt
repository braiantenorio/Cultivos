// esto es para ver la fecha de expiracion del token

const user = JSON.parse(sessionStorage.getItem("user")) 
const decoded = JSON.parse(atob(user.accessToken.split(".")[1])) 
const date = new Date(decoded.exp * 1000); 